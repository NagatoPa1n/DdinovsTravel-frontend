services:
  frontend:
    build:
      context: .
    image: ddinovstravel-frontend:latest
    container_name: ddinovstravel-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-3030}:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
